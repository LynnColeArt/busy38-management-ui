[project]
name = "busy38-management-ui"
version = "0.1.0"
description = "Management UI for Busy38 deployment control and status."
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115", "uvicorn>=0.30", "pydantic>=2.8", "python-multipart>=0.0.20"]
